### 📝 Change Log: Progetto "Castello dell'anima"

## [2026-05-06] – Uniformazione prefisso tassonomia `fase`

### Changed
- Rinominati gli `xml:id` della tassonomia `fase` dal prefisso `fase_` al prefisso `fase-`, per coerenza con la regola Schematron `category-prefix-consistency`.

### Notes
- Intervento sul `teiHeader` (tassonomia di processo `fase`); nessuna modifica a `tassonomia-gh.xml`.

---

## [2026-05-20] – Uniformazione categorie tassonomia `workflow`

### Changed
- Rinominate le 21 categorie della tassonomia `workflow` dal prefisso `scenario_*`/`quality_assurance`/`validazione` al prefisso `workflow-*`.

### Notes
- Intervento sul `teiHeader` (tassonomia di processo `workflow`); nessuna modifica a `tassonomia-gh.xml`.
